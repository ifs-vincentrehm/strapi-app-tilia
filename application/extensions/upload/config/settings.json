{
    "provider": "custom-aws-s3",
    "providerOptions": {
      "region": "${process.env.AWS_REGION}",
      "params": {
        "Bucket": "${process.env.AWS_MEDIASS3_NAME}"
      },
      "mediasCloudfrontUrl": "${process.env.AWS_MEDIAS_CLOUDFRONT_URL}"
    }
  }